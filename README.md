# ChronoSphere
ChronoSphere is a Java Swing desktop application featuring a modular command dashboard. It runs an asynchronous live clock, multi-region world time tracking, and a static lookup database for regional prayer schedules. It implements Java File I/O for persistent task tracking and active runtime memory alerts with full entry deletion controls.
